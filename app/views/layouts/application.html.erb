<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Lauralee Flores | Creating Websites People Love to Use" %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render partial: 'layouts/nav' %>
    <div class="nav-notification text-center">
      <%= render partial: 'layouts/flash' %>
    </div>

    <%= yield %>

    <%= render partial: 'layouts/footer' %>

    <!-- Modals for signing up or signing in -->
    <%= render partial: 'welcome/sign_up' %>
    <%= render partial: 'welcome/sign_in' %>

    <div class="container">
      <%= debug(params) if Rails.env.development? %>
    </div>

    <%= javascript_include_tag "application" %>
    <% if Rails.env.production? %>  
      <%= render 'layouts/ga' %>
    <% end %>

  </body>
</html>