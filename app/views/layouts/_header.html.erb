<div id="header">
	<div class="row">
		<div class="small-12 small-centered medium-6 medium-uncentered columns">
			<%= link_to "Lauralee Flores", root_path, class: "logo" %>
		</div>
		<div class="show-for-medium-up medium-6 columns text-right">
			<% if user_signed_in? %>
				<%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "button-small" %>
			<% else %>
				<%= link_to "Sign In", "#", "data-reveal-id" => "sign-in-modal", class: "ghost-button" %>
			<% end %>
		</div>
	</div>

	<!-- Intro Text -->
	<div class="header-call">
		<div class="row">
			<div class="small-12 medium-11 medium-centered columns text-center">
				<h1>Helping Entrepreneurs Create Ruby on Rails Applications that are <em>Minimal</em>, <em>Usable</em>, and <em>Persuasive</em>.</h1>
			</div>
		</div>
	</div>
		

	<% if user_signed_in? %>
		<div class="row">
			<div class="small-12 medium-8 medium-centered columns">
				<div class="small-spacer-top">
					<div class="big-spacer">
						<%= link_to "Thank You For Signing Up", comingsoon_path, class: "button expand button-pink" %>
					</div>
				</div>
			</div>
		</div>
	<% else %>
		<div id="form">
			<div class="row">
				<div class="small-12 medium-8 medium-centered columns">
					<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
						<%= devise_error_messages! %>

						<%= f.text_field :name, autofocus: true, placeholder: "First Name" %>

						<%= f.email_field :email, placeholder: "Email" %>

						<%= f.password_field :password, autocomplete: "off", placeholder: "Password" %>

						<%= f.submit "Send Me Training When It's Available", class: "button expand sign-up" %>
					<% end %>
				</div>
			</div>
		</div>
	<% end %>
</div>